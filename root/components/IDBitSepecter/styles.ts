import { pxToDp, pxToSp } from "@/utils/system";
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        backgroundColor: '#rgba(228, 228, 228, .09 )', 
        height: pxToDp(.5), 
        width: '100%', 
    },
 
})

export default styles;
